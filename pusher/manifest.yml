apiVersion: apps/v1
kind: Deployment
metadata:
  name: pusher-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pusher
  template:
    metadata:
      labels:
        app: pusher
    spec:
      containers:
      - name: pusher
        image: saumilbisht/buptime-pusher:latest